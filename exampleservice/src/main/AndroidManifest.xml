<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.servicesourcecode">

    <!--网络权限-->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--允许应用程序访问有关的网络信息-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!--允许应用程序改变网络状态-->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.BroadcastActivity"></activity>
        <activity android:name=".MainActivity"></activity>

        <service
            android:name=".service.ExampleService"
            android:enabled="true"></service>

        <activity android:name=".activity.MediaActivity"></activity>

        <service
            android:name=".service.MediaService"
            android:enabled="true"></service>
        <activity
            android:name=".activity.BookMangerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".service.BookMangerService"
            android:process=":remote"></service>

        <receiver
            android:name=".activity.BroadcastActivity"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.servicesourcecode.MY_BROADCAST"></action>
            </intent-filter>
        </receiver>
    </application>

</manifest>